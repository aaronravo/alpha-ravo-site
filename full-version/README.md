# Full version (saved for later)

This folder is a **backup of the complete Alpha Ravo site** with all sections:

- Hero (logo, headline, credibility bar, CTAs)
- Problem / Solution / How it works (5 steps) / Science / Founder
- Apply form (name, email, interest dropdown)
- Footer

**To view:** Open `index.html` in a browser. Logo and assets load from `../assets/` (parent folder).

Use this when you’re ready to publish the full story. The main `website/` folder now has the **simple launch version** (hero + email only).
